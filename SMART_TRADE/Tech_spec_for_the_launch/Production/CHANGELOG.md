# CHANGELOG (Production workspace)

## 2026-04-06 — PART 01
- Added project foundation in `Production/`.
- Added Python package scaffold and core modules (`constants`, `enums`, `errors`, `ids`, `json_contracts`).
- Added YAML+ENV configuration models and loader.
- Added default config files for single-frame baseline `binance_futures_btcusdc_1m`.
- Added `.env.example` and toolchain scaffold (`pyproject.toml`, pytest/ruff/mypy sections).
- Added smoke tests for config loading and UUIDv7 helpers.
